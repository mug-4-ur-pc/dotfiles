
# -----------------------------------------------------------------------------
# GENERAL BEHAVIOR
# -----------------------------------------------------------------------------
set window-title-basename true
set window-title-page true
set statusbar-home-tilde true
set selection-clipboard clipboard
set adjust-open best-fit
set pages-per-row 1
set page-v-padding 2
set page-h-padding 2
set page-cache-size 50
set page-thumbnail-size 16777216
set scroll-step 60
set scroll-hstep 20
set scroll-full-overlap 0.15
set scroll-wrap false
set scroll-page-aware true

# -----------------------------------------------------------------------------
# UI APPEARANCE
# -----------------------------------------------------------------------------
set font "monospace normal {{ .font.size.M }}"
set statusbar-h-padding 8
set statusbar-v-padding 2
set guioptions "shv"

# -----------------------------------------------------------------------------
# DARK THEME (Midnight Blue / Slate)
# -----------------------------------------------------------------------------
# Background colors
set default-bg "#{{ .theme.background }}"
set default-fg "#{{ .theme.foreground }}"

# Statusbar colors
set statusbar-bg "#{{ .theme.tertiary }}"            # Statusbar background
set statusbar-fg "#{{ .theme.tertiary_fg }}"            # Statusbar text

# Input/Command bar colors
set inputbar-bg "#{{ .theme.tertiary_container }}"
set inputbar-fg "#{{ .theme.tertiary_container_fg }}"

# Notification colors
set notification-bg "#{{ .theme.surface }}"
set notification-fg "#{{ .theme.surface_fg }}"
set notification-error-bg "#{{ .theme.error_container }}"
set notification-error-fg "#{{ .theme.error_container_fg }}"
set notification-warning-bg "#{{ .theme.surface }}"
set notification-warning-fg "#{{ .theme.brightyellow }}"

# Highlight/Selection colors
set highlight-color "rgba({{ template "hex_to_rgb" .theme.primary_container }}, 0.6)"
set highlight-active-color "rgba({{ template "hex_to_rgb" .theme.inverse_primary }}, 0.6)"
set highlight-fg "#{{ .theme.primary_container_fg }}"

set completion-bg "#{{ .theme.surface }}"
set completion-fg "#{{ .theme.surface_fg }}"
set completion-group-bg "#{{ .theme.surface_variant }}"
set completion-group-fg "#{{ .theme.surface_variant_fg }}"
set completion-highlight-bg "#{{ .theme.secondary }}"
set completion-highlight-fg "#{{ .theme.secondary_fg }}"

# Recolor mode (for dark PDFs)
set recolor true
set recolor-adjust-lightness false
set recolor-keephue true
set recolor-reverse-video true
set recolor-darkcolor "#{{ .theme.foreground }}"
set recolor-lightcolor "#{{ .theme.background }}"

set render-loading true
set render-loading-bg "#{{ .theme.inverse_primary }}"
set render-loading-fg "#{{ .theme.inverse_surface_fg }}"

# Index (outline) settings
set index-bg "#{{ .theme.tertiary }}"
set index-fg "#{{ .theme.tertiary_fg }}"
set index-active-bg "#{{ .theme.secondary }}"
set index-active-fg "#{{ .theme.secondary_fg }}"

# -----------------------------------------------------------------------------
# ADVANCED SETTINGS
# -----------------------------------------------------------------------------
set synctex true
set synctex-editor-command "$TERMINAL -e nvim +%{line} %{input}"
set dbus-service true
set double-click-follow true

