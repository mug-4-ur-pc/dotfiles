context.properties = {
    # Keep default at 48kHz for system sounds
    default.clock.rate = 48000

    # Enable dynamic switching to match source file rate
    # This prevents resampling when the DAC supports the rate
    default.clock.allowed-rates = [ 44100 48000 88200 96000 192000 ]
}
